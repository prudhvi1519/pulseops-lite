# UI_SPEC — Views & UX Inventory

> **Forensic Audit**
> Generated by Antigravity on 2026-01-11

## 1. UX Sitemap

### Public Pages
- **Route**: `/` (`app/page.tsx`)
  - **View**: Landing Page.
  - **Components**: Hero section, Features list, "Get Started" call to action.
  - **Auth**: Public.

- **Route**: `/login` (`app/(auth)/login/page.tsx`)
  - **View**: Email/Password form. link to Signup.
  - **Auth**: Redirects to Dashboard if session exists.

- **Route**: `/signup` (`app/(auth)/signup/page.tsx`)
  - **View**: Registration form (Name, Email, Password, Company Name).
  - **Auth**: Creates session on success.

### Dashboard (`/dashboard`)
- **Layout**: `AppShell` (Sidebar navigation + User Menu).
- **Route**: `/dashboard` (`app/dashboard/page.tsx`)
  - **View**: High-level overview.
  - **Sections**:
    - **Org Context**: Active Org Name + Role Badge.
    - **System Health**: Cards showing "Open Incidents" and "Failed Notifications".
    - **Quick Links**: Navigation to Services / Logs.
    - **Latest Deployments**: List of recent GitHub deployments.
    - **Incidents**: EmptyState / List of active incidents.
  - **Data Fetching**: Client-side `fetch('/api/me')` and `fetch('/api/diagnostics/summary')`.
  - **Loading State**: `SkeletonLoader`.

## 2. Component Inventory (`components/`)

### Core UI (`components/ui`)
| Component | Purpose | status |
|-----------|---------|--------|
| `AppShell` | Main layout wrapper (Sidebar + Content). | Stable |
| `Card` | Container with shadow/border. | Stable |
| `Badge` | Status indicator (Info/Warning/Error/Neutral). | Stable |
| `Button` | Primary/Secondary actions. | Stable |
| `EmptyState`| Placeholder for 0-data views (SVG Icon). | Stable |
| `SkeletonLoader`| Loading placeholder. | Stable |
| `PageHeader`| Title + Description + Action slot. | Stable |

### Feature Components
- `LatestDeployments`: Fetches and renders deployment timeline.
- `LogViewer` (Implied): Log explorer table/list.

## 3. UX & Accessibility Review

### Missing / Improvements Needed
1.  **Loading Waterfalls**: `DashboardPage` fetches `/api/me` and `/api/diagnostics` in parallel (good), but rendering waits for both. Could stream via Suspense if moved to Server Components.
2.  **Error Boundaries**: No visible generic ErrorBoundary component in `app/layout.tsx`.
3.  **Toasts**: No global toast provider seen (e.g. `sonner` or `react-hot-toast`) for feedback on Actions. (Check `app/layout.tsx` for providers).
4.  **Mobile Nav**: `AppShell` sidebar responsiveness verification needed.
5.  **Focus Management**: `EmptyState` actions should handle focus.

## 4. Design System
- **Framework**: Tailwind CSS.
- **Theme**: CSS Variables in `globals.css` (`--color-surface`, `--spacing-md`).
- **Typography**: System fonts (Inter/San Francisco style implied).
- **Dark Mode**: Configured via CSS variables? (Needs verification).
