# FIXLIST — Remediation Plan

> **Forensic Audit**
> Generated by Antigravity on 2026-01-11

## P0: Critical Blockers (Must Fix)
1.  **500 Internal Server Error (Homepage/Login)**:
    - **Cause**: Likely `AppShell` (Server Component) failing to connect to DB or missing `POSTGRES_URL` in the running dev process.
    - **Fix**: Verify `.env.local` loading and potentially add `error.tsx` boundary to graceful fail.
3.  **Missing DB Table (`webhook_deliveries`)**:
    - **Status**: **FIXED** (Added `0012_webhook_deliveries.sql`).
    - **Cause**: `route.ts` tries to insert, but no migration exists.
    - **Fix**: Create migration `0012_webhook_deliveries.sql`.
2.  **Skipped Tests**:
    - **Status**: **FIXED** (Updated `tests/setup.ts`).
    - **Cause**: `vitest` not loading `.env.local`.
    - **Fix**: Add `dotenv` loading to `vitest.config.ts` or `tests/setup.ts`.

## P1: Correctness & Stability
1.  **Test Failures** (`tests/api/github.webhook.signature.test.ts`):
    - **Cause**: To be determined (likely mock mismatch or signature logic).
    - **Fix**: Debug and fix expectation.
2.  **Rate Limiting on Login**:
    - **Cause**: Missing.
    - **Fix**: Add IP-based rate limiting to `/api/auth/login`.

## P2: User Experience (Polish)
1.  **Global Error Boundary**:
    - **Fix**: Add `app/global-error.tsx`.
2.  **Toast Notifications**:
    - **Fix**: Install `sonner` and add `Toaster` to `app/layout.tsx`.
3.  **ESLint Warnings**:
    - **Fix**: Remove unused disable directives.

---
**Next Actions**: Proceed to Fix P0 items.
